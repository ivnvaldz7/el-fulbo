# Handoff — 2026-04-22 (fin de sesión, feat-002 escrito)

> **Archivo crítico.** Lo primero que cualquier agente lee al arrancar sesión.

---

## Estado del proyecto

**Fase:** diseño de specs V2. Tenemos **2 features escritos de 14**. Todavía no hay código.

**Versión activa:** **V2** (pivote del 2026-04-20).

**V1 archivado** en `/specs/_archive/v1/`. No tocar.

---

## Qué está listo

### Foundation, Domain, Flows, Contracts, Quality
- ✅ Foundation V2 (`product.md`, `glossary.md`, `architecture-decisions.md`).
- ✅ Domain V2 (`entities.md`, `business-rules.md`, `balancing-algorithm.md`).
- ✅ Flows V2 (`core-flows.md`, `edge-cases.md`).
- ✅ Contracts V2 (`types.ts`, `db-schema.md`, `error-model.md`).
- ✅ Quality V2 (`testing-strategy.md`, `observability.md`).
- ✅ Backlog (`_backlog.md`).
- ✅ Engram (`decisions.json`) con **60 decisiones** activas.
- ✅ Visión central (`_v2-vision.md`).

### Features
- ✅ `feat-001-onboarding-user.md` — escrito el 2026-04-22.
- ✅ `feat-002-create-group.md` — escrito el 2026-04-22.

---

## Decisiones nuevas de la última sesión (feat-002)

Durante la sesión de feat-002 se cerraron 9 decisiones adicionales:

- **dec-052** — Form mínimo: solo nombre + modalidad (logo y donación en settings).
- **dec-053** — Admin carga stats al crear, autoaprobadas, respeta tope de 8.
- **dec-054** — Invariante: Admin NO puede editar sus propias stats más allá de 8.
- **dec-055** — Post-creación: redirect al dashboard + banner persistente hasta ≥2 Players.
- **dec-056** — Modalidad default F5 al crear grupo.
- **dec-057** — Duplicados de nombre: advertir con modal, permitir si confirma.
- **dec-058** — Form validation: error inline al tocar Crear (botón siempre activo).
- **dec-059** — Errores de creación: localStorage + mensajes específicos por causa.
- **dec-060** — Settings: accesible desde engranaje, modalidad cambiable libre, logo con crop cuadrado centrado automático.

---

## Qué falta (próxima sesión)

1. **Iván revisa `feat-002-create-group.md`** y marca ajustes si los hay.
2. **Continuar con features V2**. Próximo:
   - `feat-003-join-group.md` (jugador entra por invitación con código, flow de reactivación).
3. Seguir con el orden planificado (ver `_v2-vision.md §11`).
4. **Bootstrap del proyecto Next.js** (sugerencia: hacerlo después de tener feat-003).

---

## Decisiones V1 marcadas como superseded

- `dec-003` (Rating Interno oculto ELO) → superseded por `dec-023` (boost temporal visible).
- `dec-007` (User/Player separado) → superseded por `dec-021` (User = Player).
- `dec-010` (Parseo de lista WhatsApp) → superseded por `dec-019` (pivote a flow completo).
- `dec-014` (Magic link como auth principal) → superseded por `dec-020` (Google OAuth).

**Nota sobre dec-025:** refinada por dec-043 (tope de 8 en self-assessment).

---

## Invariantes críticas del producto

Si un agente duda, estas siempre ganan sobre cualquier ajuste puntual:

1. **`internal_rating` del V1 NO existe en V2.** Todo transparente vía `overall_actual = base + boost`.
2. **Tope de 8 en self-assessment** (dec-043). Aplica a todos los jugadores incluyendo al Admin.
3. **Admin NO puede auto-aumentar sus stats a 9-10** (dec-054). Aunque puede hacerlo con otros Players.
4. **Log público de cambios de stats** (dec-037). Solo vía feed in-app, sin push ni email.
5. **Admin inactivo:** peer pressure via sección pública "Esperando aprobación" (dec-048). NO hay auto-aprobación.
6. **Redirect silencioso al usar link de invitación siendo ya miembro** (dec-051).
7. **Modalidad default del Group no afecta events existentes** (dec-060). Cada Event tiene snapshot propio.

---

## Cosas a tener en cuenta

1. **Iván tiene la carpeta localmente ahora.** El git lo inició pero lo perdió al sobrescribir. Al recibir este paquete, debe iniciar git de nuevo y hacer primer commit real.
2. **Stack cerrado:** Next.js 14 App Router + TS strict + Tailwind + Supabase + Vercel + next-pwa + Web Push. No cambiar.
3. **Email de soporte:** `ivnvldz7@gmail.com`.
4. **Idioma:** español argentino coloquial. Sin slang juvenil, sin formalismo.
5. **Filosofía del producto:** "tan fluido como una encuesta de WhatsApp".
6. **Prohibido:** discutir chat en detalle técnico. Está en backlog.
7. **Prohibido:** cambiar tope de 8, rangos de tiers, detonantes de boost sin que Iván apruebe.
8. **Iván autorizó ir con recomendaciones por default** en decisiones técnicas/UX. Pero el debate se mantiene en decisiones que tocan filosofía del producto o contradicen datos de las entrevistas.

---

## Para el Auditor (futuro)

Al revisar cualquier PR que venga del Implementer, verificar:

- Los tipos de `/src/lib/types.ts` coinciden con `/specs/04-contracts/types.ts`.
- Tests unitarios corren y pasan (`pnpm test`).
- No se agregaron dependencias pesadas sin justificar.
- Las policies de RLS cubren todos los casos del spec.
- El código usa `Result<T, E>` y no `throw`.
- Los boosts se calculan según la tabla en `business-rules.md §5.2`.
- En feat-001: el tope de 8 en self-assessment se valida client + server. La posición híbrida DEF/MED/DEL mantiene stats, campo↔ARQ reinicia.
- En feat-002: creación atómica vía RPC `create_group`, Admin queda con stats pending_approval hasta completar wizard, wizard del Admin autoaprueba (stats_status='approved') y respeta tope de 8.
- Invariante: Admin NO puede editar sus propias stats más allá de 8 (valida en RPC de update).

---

## Próxima acción

**Iván:** revisar `feat-002-create-group.md`. Si todo OK, continuamos con `feat-003-join-group.md`.

**Claude Designer:** esperar feedback y entonces continuar con feat-003.
